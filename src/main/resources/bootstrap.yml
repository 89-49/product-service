spring:
  application:
    name: product-service

  cloud:
    config:
      uri: ${CONFIG-SERVER}
      name: product-service,common
      override-none: true

  config:
    import:
      - optional:configserver:${CONFIG-SERVER}
#      설정 서버 유레카 등록 시 변경


logging:
  level:
    org.hibernate.orm.connections.pooling: WARN
    org.hibernate.SQL: WARN

eureka:
  client:
    refresh:
      enable: false
      #      설정 서버 유레카 등록 시 변경