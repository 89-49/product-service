spring:
  application:
    name: product-service

  cloud:
    config:
      uri: ${CONFIG-SERVER:http://34.50.50.170:13100}
      name: product-service,common
      override-none: true

  config:
    import:
      - optional:configserver:${CONFIG-SERVER:http://34.50.50.170:13100}
#      설정 서버 유레카 등록 시 변경


logging:
  level:
    org.hibernate.orm.connections.pooling: WARN
    org.hibernate.SQL: WARN

eureka:
  client:
    refresh:
      enable: false
      #      설정 서버 유레카 등록 시 변경